"use client"

// Preview page — fora do (app) auth shell.
// Permite verificar a pagina Cota Sub sem precisar autenticar.
import CotaSubPage from "@/app/(app)/controladoria/cota-sub/page"

export default function PreviewCotaSub() {
  return <CotaSubPage />
}
