// Favoritos de fundo CVM (por usuario logado) — camada de dominio BI.
//
// Promovido de `(app)/bi/benchmark/_components/` em 2026-07-20, quando o
// Comparador virou o 2o consumidor (CLAUDE.md §1 regra 4: 2a pagina = promove).
// Consumidores: /bi/benchmark (FichaFundoTab) e /bi/comparador.

export { FavoritoStar } from "./FavoritoStar"
export { useFavoritos } from "./useFavoritos"
